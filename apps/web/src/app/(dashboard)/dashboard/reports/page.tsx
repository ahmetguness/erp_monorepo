import type { Metadata } from 'next';
import { ReportsPage } from '@/components/features/reports/ReportsPage';
export const metadata: Metadata = { title: 'Raporlar — Axon ERP' };
export default function Reports() { return <ReportsPage />; }
