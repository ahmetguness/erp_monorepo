import { z } from 'zod';
export const AutomationScorecardSchema = z.object({ periodDays: z.number(), generatedAt: z.string(), totals: z.object({ executions: z.number(), automaticallyCompleted: z.number(), failed: z.number(), running: z.number(), manualTouches: z.number(), recoveries: z.number(), accepted: z.number(), rejected: z.number(), corrected: z.number(), straightThroughRatePct: z.number(), successRatePct: z.number(), estimatedHoursSaved: z.number(), financialImpact: z.number() }), errorBudget: z.object({ targetSuccessRatePct: z.number(), actualSuccessRatePct: z.number(), remainingFailures: z.number(), breached: z.boolean() }), processes: z.array(z.object({ process: z.string(), executions: z.number(), succeeded: z.number(), failed: z.number(), successRatePct: z.number(), straightThroughRatePct: z.number(), estimatedMinutesSaved: z.number() })), feedbackReasons: z.array(z.object({ reason: z.string(), count: z.number() })), backlog: z.array(z.object({ executionId: z.string(), process: z.string(), status: z.enum(['RUNNING', 'FAILED']), startedAt: z.string(), error: z.string().nullable() })), reviewQueue: z.array(z.object({ executionId: z.string(), process: z.string(), startedAt: z.string() })) });
export type AutomationScorecard = z.infer<typeof AutomationScorecardSchema>;
